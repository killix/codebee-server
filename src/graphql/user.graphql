input UserInput {
  name: String!
}

type User {
  id: ID!
  name: String!
}

extend type Query {
  user(id: ID!): User
  users: [User]!
}

extend type Mutation {
  createUser(input: UserInput): User
  updateUser(id: ID!, input: UserInput): User
}
